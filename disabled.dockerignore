# exclude everything
**/*
# include normal files
!functions/*.*
!functions/node_modules
!src/*.*
!test/*.*   
!hooks/*
!chromebooks/*
!*.*
!*-*
!LICENSE
# VERSION is not sent to docker because we set version in Docker build via Docker build arg
# exclude dotfiles and other junk
**/.*
**/*.log
# re-include individual required dotfiles
!functions/.yarn*
!src/generated/.keep.directory
!functions/generated/.keep.directory
!.dockerignore
!.npmrc
!Dockerfile
